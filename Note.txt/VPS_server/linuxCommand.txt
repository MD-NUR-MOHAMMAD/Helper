নিশ্চিতভাবে! নিচে আমি Linux-এ সবচেয়ে বেশি ব্যবহৃত ও প্রয়োজনীয় কিছু কমান্ড দিচ্ছি — যা VPS বা Local Linux সার্ভারে কাজ করতে হলে জানা খুব দরকার।

---

## 🗂️ **ফাইল ও ফোল্ডার সম্পর্কিত কমান্ড**

| কাজ                         | কমান্ড                   | ব্যাখ্যা                             |
| --------------------------- | ------------------------ | ------------------------------------ |
| ফাইল/ফোল্ডার তালিকা দেখা    | `ls`                     | বর্তমান ডিরেক্টরির ফাইল দেখায়        |
| হিডেন ফাইল সহ সব দেখানো     | `ls -a`                  | সব ফাইল সহ including hidden (.) ফাইল |
| ফাইল কপি করা                | `cp source.txt dest.txt` | `source.txt` থেকে `dest.txt` বানাবে  |
| ফাইল/ফোল্ডার স্থানান্তর     | `mv old.txt new.txt`     | নাম পরিবর্তন বা move করা             |
| ফাইল ডিলিট করা              | `rm file.txt`            | ফাইল মুছে ফেলা                       |
| ফোল্ডার ডিলিট করা           | `rm -r foldername`       | রিকার্সিভভাবে ফোল্ডার মুছে ফেলা      |
| ফোল্ডার তৈরি করা            | `mkdir myfolder`         | নতুন ফোল্ডার তৈরি                    |
| অবস্থান পরিবর্তন (navigate) | `cd foldername/`         | উক্ত ফোল্ডারে চলে যাওয়া              |
| বর্তমান অবস্থান দেখা        | `pwd`                    | আপনি কোথায় আছেন তা দেখায়             |

---

## 🔐 **পারমিশন ও ইউজার সম্পর্কিত কমান্ড**

| কাজ               | কমান্ড                      | ব্যাখ্যা                      |
| ----------------- | --------------------------- | ----------------------------- |
| ফাইল পারমিশন দেখা | `ls -l`                     | পারমিশন ও মালিকানা দেখায়      |
| পারমিশন পরিবর্তন  | `chmod 755 file.txt`        | ফাইলের পারমিশন বদলায়          |
| মালিকানা পরিবর্তন | `chown user:group file.txt` | ইউজার/গ্রুপ মালিকানা পরিবর্তন |
| ইউজার তৈরি করা    | `adduser newuser`           | নতুন ইউজার তৈরি               |
| ইউজার ডিলিট করা   | `deluser olduser`           | ইউজার মুছে ফেলা               |

---

## 🔍 **সিস্টেম ও প্রসেস সম্পর্কিত কমান্ড**

| কাজ                    | কমান্ড                    | ব্যাখ্যা                              |
| ---------------------- | ------------------------- | ------------------------------------- |
| RAM/CPU দেখা           | `top` বা `htop`           | চলমান প্রসেস ও রিসোর্স ব্যবহার        |
| সিস্টেম ইনফো দেখা      | `uname -a`                | কার্নেল ও সিস্টেম তথ্য                |
| ডিস্ক ব্যবহার দেখা     | `df -h`                   | কোন পার্টিশনে কত জায়গা আছে            |
| RAM ব্যবহার দেখা       | `free -h`                 | RAM ও swap usage                      |
| সার্ভিস চালু করা       | `systemctl start nginx`   | নির্দিষ্ট সার্ভিস চালানো (যেমন nginx) |
| সার্ভিস বন্ধ করা       | `systemctl stop nginx`    | সার্ভিস বন্ধ                          |
| সার্ভিস রিস্টার্ট করা  | `systemctl restart nginx` | সার্ভিস রিস্টার্ট                     |
| সার্ভিস স্ট্যাটাস দেখা | `systemctl status nginx`  | সার্ভিস চলছে কিনা দেখা                |

---

## 🌐 **নেটওয়ার্ক সম্পর্কিত কমান্ড**

| কাজ                | কমান্ড               | ব্যাখ্যা                           |
| ------------------ | -------------------- | ---------------------------------- |
| IP দেখা            | `ip a` বা `ifconfig` | সার্ভারের IP অ্যাড্রেস দেখা        |
| সার্ভার পিং করা    | `ping google.com`    | ইন্টারনেট কানেকশন ঠিক আছে কিনা চেক |
| পোর্ট ব্যবহার দেখা | `netstat -tulpn`     | কোন পোর্টে কি সার্ভিস চলছে         |

---

## 🧪 **উদাহরণ: Laravel এর জন্য দরকারি কিছু কমান্ড**

```bash
cd /var/www/laravel-app      # অ্যাপ ফোল্ডারে যাওয়া
chmod -R 775 storage         # storage ফোল্ডারে পারমিশন দেওয়া
php artisan migrate          # মাইগ্রেশন চালানো
php artisan serve            # সার্ভার চালানো (local dev)
```

---

আপনি যদি বলেন আপনি কি প্রজেক্টে কাজ করছেন (যেমন Laravel, Node.js, ইত্যাদি), তাহলে আমি সেই অনুযায়ী দরকারি Linux কমান্ড সেট সাজিয়ে দিতে পারি। চাইলে প্র্যাকটিস করার জন্য একটা VPS walkthrough ও দিতে পারি। বলুন কীভাবে সাহায্য করতে পারি?
